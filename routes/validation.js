var express = require('express');
var router = express.Router();
// var validator = require('node-validator');

// var check = validator.isString({ regex: /^[abc]+$/ , message: 'Invalid value. Value must be hex.'});

// module.exports = {
// 	check : check
// }
